<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--사용하는 repo경로를 적어줌-->
<mapper namespace="com.bp.mybatisdb.dao.TestDao">

    <select id="getUserList" resultType="com.bp.mybatisdb.dto.TestDto">
        SELECT * FROM test;
    </select>

</mapper>